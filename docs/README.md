# Google AI Edge Gallery - Android 核心架构

**Google AI Edge Gallery** 是一个端侧生成式 AI 应用，强调离线推理、低延迟和隐私保护。本文档系统性地剖析其 Android 应用架构。

---

## 📚 核心文档（推荐顺序）

### 第一部分：应用基础

| 章节 | 内容 | 关键概念 |
|------|------|--------|
| **启动与应用层** | MainActivity 入口、Splash 页面、Deep Link 处理、全局初始化 | `MainActivity` \| `GalleryApplication` \| Hilt 依赖注入 |
| **导航与页面流** | 导航架构、页面转换、Deep Link 路由、页面栈管理 | `GalleryNavGraph` \| Jetpack Navigation \| 页面流程 |

### 第二部分：核心系统

| 章节 | 内容 | 关键概念 |
|------|------|--------|
| **数据与状态层** | 持久化存储、状态管理、仓储模式、任务定义 | `DataStoreRepository` \| `Task`/`Category` \| StateFlow |
| **推理运行时层** | LiteRT-LM 和 MLKit AICore 框架、模型初始化、推理执行 | `LlmModelHelper` \| Engine \| GenerativeModel \| 流式推理 |

### 第三部分：功能扩展

| 章节 | 内容 | 关键概念 |
|------|------|--------|
| **任务扩展层** | 自定义任务框架、Agent Chat、技能管理、MCP 集成 | `CustomTask` 接口 \| Skills \| MCP Protocol |
| **UI 组件层** | 可复用组件、聊天界面、模型卡片、主题系统 | Jetpack Compose \| 聊天气泡 \| 主题切换 |

### 第四部分：系统支撑

| 章节 | 内容 | 关键概念 |
|------|------|--------|
| **系统与后台** | 后台任务、模型下载、通知管理、性能优化 | WorkManager \| DownloadWorker \| 通知调度 |

---

## 🎯 快速导航

### 按场景查阅

- **我要理解整个应用从启动到用户交互的流程**  
  → 阅读：启动与应用层 → 导航与页面流 → 首次使用完整流程

- **我要扩展新的 AI 功能或集成新模型**  
  → 阅读：推理运行时层 → 数据与状态层 → 任务扩展层

- **我要自定义一个新任务（如对话、工具调用等）**  
  → 阅读：任务扩展层 → UI 组件层 → 系统与后台

- **我想优化推理性能或支持新的加速器**  
  → 阅读：推理运行时层 → 系统与后台

---

## 📖 实操补充

- **[首次使用完整流程](./08-first-time-user-end-to-end.md)**：端到端用户体验、模型选择、下载、推理、会话管理

---

## 🏗️ 架构全景图

```
┌─────────────────────────────────────────────────────────────┐
│                    UI 层（Compose）                         │
│  ┌─────────────┬──────────────┬──────────────────────────┐ │
│  │ Chat Screen │ Model Manager │ Benchmark / Custom Task  │ │
│  └─────────────┴──────────────┴──────────────────────────┘ │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│              ViewModel 层（状态管理）                       │
│  • LlmChatViewModel  • ModelManagerViewModel              │
│  • 通过 StateFlow 驱动 UI  • 处理用户交互                 │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│          推理运行时层（LlmModelHelper）                    │
│  ┌──────────────────────┬──────────────────────────────┐  │
│  │  LiteRT-LM Backend   │  MLKit AICore Backend        │  │
│  │ • EngineConfig      │ • GenerativeModel             │  │
│  │ • Conversation      │ • ChatHistory                 │  │
│  └──────────────────────┴──────────────────────────────┘  │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│        数据与仓储层（Repository）                         │
│  • DataStoreRepository（持久化设置/历史）                 │
│  • DownloadRepository（模型下载/管理）                    │
│  • 模型元数据/配置读取                                    │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│        系统支撑（WorkManager、通知、权限等）              │
└────────────────────────────────────────────────────────────┘
```

---

**开始阅读：** 建议从 **启动与应用层** 开始，按照上表顺序逐章深入。
